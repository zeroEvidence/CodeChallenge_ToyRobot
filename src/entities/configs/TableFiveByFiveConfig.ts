import { ISurfaceConfig } from "../surface/interfaces/SurfaceConfig.interface";

/**
 * TableFiveByFiveConfig is the configuration object that is used to produce the
 * standard 5x5 table.
 */
export const TableFiveByFiveConfig: ISurfaceConfig = {
  length: 5,
  width: 5
};
